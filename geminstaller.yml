# GemInstaller config file which specifies the actual build and test dependencies for GemInstaller itself.
defaults:
  install_options: --include-dependencies
gems:
  - name: mongrel
    version: > 0.3.13.2
    platform: ruby
